from distutils.core import setup
setup(
      name = 'expectorant',
      packages = ['expectorant'], # this must be the same as the name above
      version = '0.1',
      description = 'RSpec style testing framework for Python',
      author = 'Winston Wolff',
      author_email = 'winston@nitidbit.com',
      url = 'https://github.com/winstonwolff/expectorant', # use the URL to the github repo
      download_url = 'https://github.com/winstonwolff/expectorant/tarball/0.1', # I'll explain this in a second
      keywords = ['testing', 'rspec', 'tdd'], # arbitrary keywords
      classifiers = [],
)

